{
  "name": "logos-daily-backend",
  "version": "1.0.0",
  "description": "Backend API for Logos Daily Bible App",
  "main": "server.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build, npm run build",
    "preview": "vite preview",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "start": "node server.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }

}
